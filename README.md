docker-protractor-cucumber-example
==================================

An example of how to setup and build, test and run a node js application inside a docker container

Requirements:

* Ruby
* Vagrant
* VirtualBox
* Docker
